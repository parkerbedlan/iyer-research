import type { NextPage } from "next";
import { Layout } from "../components/Layout";

const PhotosPage: NextPage = () => {
  return (
    <Layout title="Photos" background="cell5">
      This is the Photos Page
    </Layout>
  );
};

export default PhotosPage;
